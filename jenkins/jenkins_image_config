pipeline {

    agent any
    
    environment {
        AWS_KEY = ''
        AWS_SECRET_KEY = ''
        AWS_SECRET_ACCESS_KEY = ''
        GIT_REPO_URL = ''
        SSH_KEY = ''
    }

    stages {
        stage('Hello World') {
            steps {
                echo "Hello from outside"
            }
        }

        stage('Code CHECKOUT') {
            steps {
                echo "cloning code base"
                git branch: 'praj', url:'https://github.com/garimas007/StreamingApp.git'
            }
        }

        stage('Setting up git') {
            steps {

            }
        }


    }
}